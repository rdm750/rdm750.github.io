def search(arr,key,lo,hi,flag):
...      if flag:
...              return mid
...      else:
...              if lo>=len(arr):
...                      return -1
...              else:
...                      return lo,hi

