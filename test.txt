
'''
516  git config --list
  517  cd rdm750*
  518  pwd
  519  echo 'Hello World' > index.html
  520  git add --all
  521  git commit -m 'initial commit'
  522  git push -u origin master

'''

process to add modifed local files to remote github
add all; commit; push to master branch
584  git add --a
  585  git commit -m 'initial commit'
  586  git status
  587  git push origin master
